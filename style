/*  Import Google Fonts (Optional - Example) */
@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&family=Roboto:wght@400;500;700&display=swap');

/* Global Resets & Base Styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    text-decoration: none;
    scroll-behavior: smooth;
}

:root {
    --primary-color: crimson; /* CHANGE THIS TO YOUR BRAND COLOR */
    --secondary-color: #333;
    --text-color: #333;
    --light-text-color: #555;
    --bg-light: #f8f9fa;
    --bg-white: #fff;
    --border-color: #e0e0e0;
    --box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
    --box-shadow-light: 0 2px 8px rgba(0, 0, 0, 0.08);
    --font-primary: 'Poppins', sans-serif;
    --font-secondary: 'Roboto', sans-serif;
}

html {
    scroll-padding-top: 70px; /* Adjust based on navbar height */
}

body {
    font-family: var(--font-primary);
    color: var(--text-color);
    line-height: 1.6;
}

/* Utility Classes */
.max-width {
    max-width: 1300px;
    padding: 0 40px; /* Adjusted for better spacing */
    margin: auto;
}

/* All Similar Sections Styling */
section {
    padding: 100px 0;
}

section .title {
    position: relative;
    text-align: center;
    font-size: 40px;
    font-weight: 500;
    margin-bottom: 60px;
    padding-bottom: 20px;
    font-family: var(--font-secondary);
}

section .title::before {
    content: "";
    position: absolute;
    bottom: 0px;
    left: 50%;
    width: 180px;
    height: 3px;
    background: var(--text-color);
    transform: translateX(-50%);
}

section .title::after {
    content: "who i am / what i do / what i offer"; /* Generic - you can make this dynamic or section-specific */
    position: absolute;
    bottom: -12px;
    left: 50%;
    font-size: 20px;
    color: var(--primary-color);
    padding: 0 5px;
    background: var(--bg-white); /* Or section background */
    transform: translateX(-50%);
}
/* Adjust section .title::after background for sections with non-white backgrounds */
.about, .services, .skills, .portfolio, .experience, .teams, .testimonials, .contact {
    background: var(--bg-white); /* Default, change if needed */
}
.services, .skills, .teams, .experience { /* Example for sections with light gray background */
    background: var(--bg-light);
}
.services .title::after,
.skills .title::after,
.teams .title::after,
.experience .title::after {
    background: var(--bg-light);
}


/* Navbar Styling */
.navbar {
    position: fixed;
    width: 100%;
    z-index: 999;
    padding: 15px 0; /* Reduced padding for sticky */
    font-family: var(--font-secondary);
    transition: all 0.3s ease;
    background: var(--primary-color); /* Initial navbar background */
}

.navbar.sticky {
    padding: 10px 0; /* Even less padding when sticky */
    background: var(--primary-color); /* Or a different color when sticky */
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
}

.navbar .max-width {
    display: flex;
    align-items: center;
    justify-content: space-between;
}

.navbar .logo a {
    color: var(--bg-white);
    font-size: 35px;
    font-weight: 600;
}

.navbar .logo a span {
    color: var(--secondary-color); /* Or another accent color for the span */
    transition: all 0.3s ease;
}

.navbar.sticky .logo a span {
    color: var(--bg-white); /* Change span color on sticky if needed */
}

.navbar .menu li {
    list-style: none;
    display: inline-block;
}

.navbar .menu li a {
    display: block;
    color: var(--bg-white);
    font-size: 18px;
    font-weight: 500;
    margin-left: 25px;
    transition: color 0.3s ease;
}

.navbar .menu li a:hover {
    color: var(--secondary-color); /* Or another hover color */
}

.navbar.sticky .menu li a:hover {
    color: #ddd; /* Hover color for sticky navbar links */
}

/* Menu Btn Styling */
.menu-btn-toggler {
    color: var(--bg-white);
    font-size: 23px;
    cursor: pointer;
    display: none;
}

.scroll-up-btn {
    position: fixed;
    height: 45px;
    width: 42px;
    background: var(--primary-color);
    right: 30px;
    bottom: 10px;
    text-align: center;
    line-height: 45px;
    color: var(--bg-white);
    z-index: 9999;
    font-size: 30px;
    border-radius: 6px;
    border-bottom-width: 2px;
    cursor: pointer;
    opacity: 0;
    pointer-events: none;
    transition: all 0.3s ease;
}

.scroll-up-btn.show {
    bottom: 30px;
    opacity: 1;
    pointer-events: auto;
}

.scroll-up-btn:hover {
    filter: brightness(90%);
}

/* Home Section Styling */
.home {
    display: flex;
    background: url("images/banner-placeholder.jpg") no-repeat center; /* REPLACE WITH YOUR BANNER IMAGE */
    height: 100vh;
    color: var(--bg-white);
    min-height: 500px;
    background-size: cover;
    background-attachment: fixed; /* Parallax effect */
    font-family: var(--font-primary);
}

.home .max-width {
    width: 100%;
    display: flex;
    align-items: center; /* Vertically center content */
    justify-content: center; /* Horizontally center content if text-align doesn't do it */
    text-align: center;
}
.home .home-content {
    text-align: center;
}
.home .home-content .text-1 {
    font-size: 27px;
}

.home .home-content .text-2 {
    font-size: 75px;
    font-weight: 600;
    margin-left: -3px;
}

.home .home-content .text-3 {
    font-size: 40px;
    margin: 5px 0;
}

.home .home-content .text-3 span {
    color: var(--primary-color);
    font-weight: 500;
}

.home .home-content a.cta-button,
.about .about-content .right a.cta-button,
.skills .skills-content .left a.cta-button-outline {
    display: inline-block;
    background: var(--primary-color);
    color: var(--bg-white);
    font-size: 20px; /* Reduced from 25px */
    padding: 10px 25px; /* Reduced from 12px 36px */
    margin-top: 20px;
    font-weight: 500;
    border-radius: 6px;
    border: 2px solid var(--primary-color);
    transition: all 0.3s ease;
}

.home .home-content a.cta-button:hover,
.about .about-content .right a.cta-button:hover {
    color: var(--primary-color);
    background: none;
}
.skills .skills-content .left a.cta-button-outline {
    background: none;
    color: var(--primary-color);
}
.skills .skills-content .left a.cta-button-outline:hover {
    background: var(--primary-color);
    color: var(--bg-white);
}


/* About Section Styling */
.about .about-content,
.skills .skills-content,
.contact .contact-content {
    display: flex;
    align-items: flex-start; /* Align items to the top */
    justify-content: space-between;
    flex-wrap: wrap; /* Allow wrapping on smaller screens */
}

.about .about-content .column,
.skills .skills-content .column,
.contact .contact-content .column {
    margin-bottom: 20px; /* Add space when columns stack */
}

.about .about-content .left {
    width: 45%;
}

.about .about-content .left img {
    height: 400px;
    width: 400px;
    object-fit: cover;
    border-radius: 6px;
    box-shadow: var(--box-shadow);
}

.about .about-content .right {
    width: 55%;
}

.about .about-content .right .text {
    font-size: 25px;
    font-weight: 600;
    margin-bottom: 10px;
}

.about .about-content .right .text span {
    color: var(--primary-color);
}

.about .about-content .right p {
    text-align: justify;
    color: var(--light-text-color);
}


/* Services, Portfolio, Teams, Testimonials Section Card Styling */
.services .serv-content,
.portfolio .portfolio-content, /* Assuming owl-carousel is inside this */
.teams .carousel,             /* Owl carousel is directly this class */
.testimonials .carousel {     /* Owl carousel is directly this class */
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between; /* Default for non-carousel */
}

.services .serv-content .card,
.portfolio .portfolio-content .card,
.teams .carousel .card,
.testimonials .carousel .card {
    width: calc(33% - 20px); /* Adjust for gap */
    background: var(--bg-white);
    text-align: center;
    border-radius: 6px;
    padding: 25px; /* Increased padding */
    margin-bottom: 20px; /* Space between rows */
    cursor: default; /* Changed from pointer as they are not all links */
    transition: all 0.3s ease;
    box-shadow: var(--box-shadow-light);
    border: 1px solid var(--border-color);
}

.services .serv-content .card:hover,
.portfolio .portfolio-content .card:hover,
.teams .carousel .card:hover,
.testimonials .carousel .card:hover {
    background: var(--primary-color);
    transform: translateY(-10px);
    box-shadow: var(--box-shadow);
}

.services .serv-content .card .box,
.portfolio .portfolio-content .card .box,
.teams .carousel .card .box,
.testimonials .carousel .card .box {
    transition: all 0.3s ease;
}

.services .serv-content .card:hover .box,
.portfolio .portfolio-content .card:hover .box,
.teams .carousel .card:hover .box,
.testimonials .carousel .card:hover .box {
    transform: scale(1.05);
}

.services .serv-content .card i,
.portfolio .portfolio-content .card i, /* If portfolio cards have icons */
.teams .carousel .card i,             /* If team cards have icons */
.testimonials .carousel .card i {     /* If testimonial cards have icons */
    font-size: 50px;
    color: var(--primary-color);
    transition: color 0.3s ease;
}

.services .serv-content .card:hover i,
.portfolio .portfolio-content .card:hover i,
.teams .carousel .card:hover i,
.testimonials .carousel .card:hover i {
    color: var(--bg-white);
}

.services .serv-content .card .text,
.portfolio .portfolio-content .card .text,
.teams .carousel .card .text,
.testimonials .carousel .card .text {
    font-size: 25px;
    font-weight: 500;
    margin: 10px 0 7px 0;
    color: var(--secondary-color);
    transition: color 0.3s ease;
}

.services .serv-content .card:hover .text,
.portfolio .portfolio-content .card:hover .text,
.teams .carousel .card:hover .text,
.testimonials .carousel .card:hover .text {
    color: var(--bg-white);
}

.services .serv-content .card p,
.portfolio .portfolio-content .card p,
.teams .carousel .card p,
.testimonials .carousel .card p {
    color: var(--light-text-color);
    transition: color 0.3s ease;
    font-size: 15px;
}
.services .serv-content .card:hover p,
.portfolio .portfolio-content .card:hover p,
.teams .carousel .card:hover p,
.testimonials .carousel .card:hover p {
    color: #f2f2f2;
}

/* Portfolio Specific */
.portfolio .section-subtitle {
    text-align: center;
    margin-top: -40px;
    margin-bottom: 40px;
    font-size: 18px;
    color: var(--light-text-color);
}
.portfolio .project-card img {
    width: 100%;
    height: 200px; /* Adjust as needed */
    object-fit: cover;
    border-radius: 4px;
    margin-bottom: 15px;
}
.portfolio .project-card .project-link {
    display: inline-block;
    margin-top: 15px;
    padding: 8px 15px;
    background: var(--secondary-color);
    color: var(--bg-white);
    border-radius: 4px;
    font-size: 14px;
    transition: background 0.3s ease;
}
.portfolio .project-card:hover .project-link {
    background: var(--bg-white);
    color: var(--primary-color);
}
.portfolio .project-card .project-link i {
    margin-left: 5px;
    font-size: 12px; /* Resetting icon size if it inherited from service icon */
    color: inherit !important; /* Force color inheritance */
}


/* Experience Section Styling */
.experience .experience-content {
    display: flex;
    flex-direction: column;
    gap: 40px;
}

.experience .experience-item {
    background: var(--bg-white);
    padding: 25px 30px;
    border-radius: 8px;
    box-shadow: var(--box-shadow-light);
    border-left: 5px solid var(--primary-color); /* Accent border */
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.experience .experience-item:hover {
    transform: translateY(-5px);
    box-shadow: var(--box-shadow);
}

.experience .experience-header .job-title {
    font-size: 22px;
    font-weight: 600;
    color: var(--primary-color);
    margin-bottom: 5px;
}

.experience .experience-header .company-name {
    font-size: 18px;
    font-weight: 500;
    color: var(--secondary-color);
    margin-bottom: 5px;
}

.experience .experience-header .company-name .job-location {
    font-weight: 400;
    font-style: italic;
    color: var(--light-text-color);
}

.experience .experience-header .job-dates {
    font-size: 15px;
    color: #777;
    margin-bottom: 15px;
}

.experience .experience-details > p { /* Targeting the "Key Responsibilities..." paragraph */
    font-size: 16px;
    font-weight: 500;
    margin-bottom: 10px;
    color: var(--secondary-color);
}

.experience .experience-details ul {
    list-style: disc;
    margin-left: 20px;
    padding-left: 10px;
}

.experience .experience-details ul li {
    font-size: 16px;
    line-height: 1.7;
    color: var(--light-text-color);
    margin-bottom: 8px;
}

/* Skills Section Styling */
.skills .skills-content .column {
    margin-bottom: 30px; /* Space when columns stack */
}
.skills .skills-content .left {
    width: calc(50% - 30px);
}
.skills .skills-content .left .text {
    font-size: 20px;
    font-weight: 600;
    margin-bottom: 10px;
}

.skills .skills-content .left p {
    text-align: justify;
    color: var(--light-text-color);
}

.skills .skills-content .right {
    width: calc(50% - 30px);
}
.skills .skills-content .right .bars {
    margin-bottom: 15px;
}

.skills .skills-content .right .info {
    display: flex;
    margin-bottom: 5px;
    align-items: center;
    justify-content: space-between;
}

.skills .skills-content .right span {
    font-weight: 500;
    font-size: 18px;
    color: var(--secondary-color);
}

.skills .skills-content .right .line {
    height: 5px;
    width: 100%;
    background: lightgrey;
    position: relative;
    border-radius: 3px;
}

.skills .skills-content .right .line::before {
    content: "";
    position: absolute;
    height: 100%;
    left: 0;
    top: 0;
    background: var(--primary-color);
    border-radius: 3px;
}

/* Assign widths for skill bars (can also be set by JavaScript) */
.skills .skills-content .right .electrical-skill-1::before { width: 90%; }
.skills .skills-content .right .electrical-skill-2::before { width: 85%; }
.skills .skills-content .right .html::before { width: 70%; }
.skills .skills-content .right .css::before { width: 60%; }
.skills .skills-content .right .digital-marketing::before { width: 95%; }
.skills .skills-content .right .seo::before { width: 80%; }
.skills .skills-content .right .pr::before { width: 75%; }
.skills .skills-content .right .js::before { width: 50%; }


/* Teams Section Styling */
.teams .carousel .card {
    background: var(--bg-white); /* Override hover effect if not desired for teams */
    padding: 20px;
    border-radius: 6px;
    /* transition: none; */ /* Remove if you want the hover effect */
}
.teams .carousel .card:hover {
    /* background: var(--primary-color); */ /* Uncomment for hover */
}
.teams .carousel .card .box {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
}

.teams .carousel .card img {
    height: 150px;
    width: 150px;
    object-fit: cover;
    border-radius: 50%;
    border: 5px solid var(--primary-color);
    transition: all 0.3s ease;
    margin-bottom: 10px;
}

.teams .carousel .card:hover img {
    border-color: var(--bg-white);
}

.teams .carousel .card .text {
    font-size: 22px; /* Reduced from 25px */
}
.teams .carousel .card p {
    font-size: 14px; /* Reduced from 15px */
}

/* Testimonials Section Styling */
.testimonials .carousel .card {
    padding: 30px;
}
.testimonials .carousel .card img {
    height: 120px;
    width: 120px;
    object-fit: cover;
    border-radius: 50%;
    border: 4px solid var(--primary-color);
    margin: 0 auto 20px auto; /* Center image */
    display: block;
}
.testimonials .carousel .card p { /* The quote */
    font-style: italic;
    font-size: 17px; /* Slightly larger for quotes */
    margin-bottom: 10px;
}
.testimonials .carousel .card span { /* Client title/company */
    display: block;
    font-size: 15px;
    font-weight: 500;
    color: var(--primary-color);
}

/* Contact Section Styling */
.contact .contact-content .column {
    margin-bottom: 30px; /* Space when columns stack */
}
.contact .contact-content .left {
    width: calc(50% - 30px);
}

.contact .contact-content .left .text {
    font-size: 20px;
    font-weight: 600;
    margin-bottom: 10px;
}

.contact .contact-content .left p {
    text-align: justify;
    color: var(--light-text-color);
}

.contact .contact-content .left .icons {
    margin: 10px 0;
}

.contact .contact-content .row {
    display: flex;
    height: 65px;
    align-items: center;
}

.contact .contact-content .row .info {
    margin-left: 30px;
}

.contact .contact-content .row i {
    font-size: 25px;
    color: var(--primary-color);
}

.contact .contact-content .info .head {
    font-weight: 500;
    color: var(--secondary-color);
}

.contact .contact-content .info .sub-title {
    color: var(--light-text-color);
}

.contact .contact-content .right {
    width: calc(50% - 30px);
}
.contact .right .text {
    font-size: 20px;
    font-weight: 600;
    margin-bottom: 10px;
}

.contact .right form .fields {
    display: flex;
}

.contact .right form .field,
.contact .right form .fields .field {
    height: 45px;
    width: 100%;
    margin-bottom: 15px;
}

.contact .right form .textarea {
    height: 80px;
    width: 100%;
}

.contact .right form .name {
    margin-right: 10px;
}

.contact .right form .email {
    margin-left: 10px;
}

.contact .right form .field input,
.contact .right form .textarea textarea {
    height: 100%;
    width: 100%;
    border: 1px solid lightgrey;
    border-radius: 6px;
    outline: none;
    padding: 0 15px;
    font-size: 17px;
    font-family: var(--font-primary);
    transition: border-color 0.3s ease;
}

.contact .right form .field input:focus,
.contact .right form .textarea textarea:focus {
    border-color: var(--primary-color);
}

.contact .right form .textarea textarea {
    padding-top: 10px;
    resize: none;
}

.contact .right form .button-area {
    display: flex;
    align-items: center;
}

.contact .right form .button-area button {
    color: #fff;
    display: block;
    width: 160px!important;
    height: 45px;
    outline: none;
    font-size: 18px;
    font-weight: 500;
    border-radius: 6px;
    cursor: pointer;
    flex-wrap: nowrap;
    background: var(--primary-color);
    border: 2px solid var(--primary-color);
    transition: all 0.3s ease;
}

.contact .right form .button-area button:hover {
    color: var(--primary-color);
    background: none;
}

/* Footer Styling */
footer {
    background: var(--secondary-color);
    padding: 15px 23px;
    color: #fff;
    text-align: center;
}
footer .max-width {
    display: flex;
    flex-direction: column;
    align-items: center;
}
footer .footer-content {
    width: 100%;
}
footer span a {
    color: var(--primary-color);
    text-decoration: none;
}

footer span a:hover {
    text-decoration: underline;
}
footer .social-links {
    margin-top: 10px;
}
footer .social-links a {
    color: #fff;
    font-size: 20px;
    margin: 0 8px;
    transition: color 0.3s ease;
}
footer .social-links a:hover {
    color: var(--primary-color);
}

/* Owl Carousel Navigation/Dots Styling (Optional Overrides) */
.owl-dots {
    text-align: center;
    margin-top: 20px;
}
.owl-dot {
    height: 13px;
    width: 13px;
    margin: 0 5px;
    outline: none!important;
    border-radius: 50%;
    border: 2px solid var(--primary-color)!important;
    transition: all 0.3s ease;
}
.owl-dot.active {
    width: 35px;
    border-radius: 14px;
}
.owl-dot.active,
.owl-dot:hover {
    background: var(--primary-color)!important;
}


/* Responsive Media Query */
@media (max-width: 1104px) {
    .about .about-content .left img {
        height: 350px;
        width: 350px;
    }
}

@media (max-width: 991px) {
    .max-width {
        padding: 0 25px; /* Reduced padding */
    }
    .home .home-content .text-2 { font-size: 70px; }
    .home .home-content .text-3 { font-size: 35px; }
    .home .home-content a.cta-button { font-size: 18px; padding: 8px 20px; }

    .services .serv-content .card,
    .portfolio .portfolio-content .card,
    .teams .carousel .card,
    .testimonials .carousel .card {
        width: calc(50% - 10px); /* 2 cards per row */
    }
    .skills .skills-content .column,
    .contact .contact-content .column {
        width: 100%; /* Stack columns */
    }
    .skills .skills-content .left { margin-bottom: 35px; }
    .about .about-content .left {
        display: flex;
        justify-content: center;
        margin: 0 auto 60px;
    }
    .about .about-content .right {
        flex: 100%;
    }
}

@media (max-width: 768px) { /* Common tablet/mobile breakpoint */
    .max-width {
        padding: 0 20px;
    }
    section .title { font-size: 35px; }
    section .title::after { font-size: 17px; }

    .menu-btn-toggler {
        display: block;
        z-index: 999;
    }

    .navbar .menu {
        position: fixed;
        height: 100vh;
        width: 100%;
        left: -100%; /* Initially off-screen */
        top: 0;
        background: var(--secondary-color); /* Dark background for mobile menu */
        text-align: center;
        padding-top: 80px;
        transition: all 0.3s ease;
    }

    .navbar .menu.active {
        left: 0; /* Slide in */
    }

    .navbar .menu li {
        display: block;
    }

    .navbar .menu li a {
        display: inline-block;
        margin: 20px 0;
        font-size: 25px;
        color: var(--bg-white); /* Ensure links are visible */
    }
    .navbar .menu li a:hover {
        color: var(--primary-color); /* Hover for mobile menu links */
    }

    .home .home-content .text-2 { font-size: 60px; }
    .home .home-content .text-3 { font-size: 32px; }

    .services .serv-content .card,
    .portfolio .portfolio-content .card,
    .teams .carousel .card,
    .testimonials .carousel .card {
        width: 100%; /* 1 card per row on smaller screens */
        margin-bottom: 20px;
    }
    .contact .right form .fields {
        flex-direction: column;
    }
    .contact .right form .name,
    .contact .right form .email {
        margin: 0 0 15px 0;
    }
}

@media (max-width: 500px) {
    .max-width {
        padding: 0 15px;
    }
    section .title { font-size: 30px; }
    section .title::after { font-size: 15px; padding: 0 3px; bottom: -10px;}

    .home .home-content .text-2 { font-size: 50px; }
    .home .home-content .text-3 { font-size: 27px; }
    .about .about-content .right .text,
    .skills .skills-content .left .text { font-size: 19px; }

    .scroll-up-btn { right: 15px; bottom: 15px; height: 38px; width: 35px; font-size: 23px; line-height: 38px; }
}
